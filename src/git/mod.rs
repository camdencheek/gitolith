// use std::collections::HashMap;

// use crate::{cache::Cache, shard::cached::CachedShard};

// pub struct GitIndex {
//     shards: Vec<CachedShard>,
//     cache: Cache,
// }

// impl GitIndex {
//     fn open()
// }
